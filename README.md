# Nathan Michira Gesora — Portfolio

Static Next.js developer portfolio. Start with `PRD.md`, then `ARCHITECTURE-ESSENTIALS.md`.
Coding agents: read `CLAUDE.md` (canonical instructions — `AGENTS.md` just points to it).

## Quick start
```bash
npm install
cp .env.example .env.local   # fill in RESEND_API_KEY, CONTACT_TARGET_EMAIL
npm run dev
```

## Status
Scaffold stage — data files have real project content, components are structurally
drafted but not styled/wired. See TODOs throughout `src/` and `PRD.md` §10.
